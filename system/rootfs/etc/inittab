# MIMIKI - Busybox inittab
# /etc/inittab: init(8) configuration for busybox

# Bring it up
::sysinit:/etc/init.d/rcS

# Keep launcher alive (via cage Wayland compositor)
::respawn:/bin/sh -l -c "/usr/bin/mimiki-launcher"

# Break it down
::shutdown:/etc/init.d/rcK
