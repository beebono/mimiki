# MIMIKI - Busybox inittab
# /etc/inittab: init(8) configuration for busybox

# Bring it up
::sysinit:/etc/init.d/rcS

# Keep launcher alive
::respawn:/bin/sh -l -c "/usr/bin/mimiki-launcher 2>&1 | tee /mnt/games/data/mimiki.log"

# Break it down
::shutdown:/etc/init.d/rcK
